# cuRobo continuous and stopped-path timing

135 independently audited native worker exports plus 135 unchanged original reference recordings. The stopped-path fallback preserves waypoint stops when blending cannot produce an acceptable faster path. Source hashes and enabled options are in manifest.json. All compute/resource comparisons are excluded. Travel and RMS joint jerk comparisons appear in report.html; faster motion can increase jerk while remaining within recorded limits.
